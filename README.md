# accident-detection-transformer
Accident Detection using EfficientNetB1 + Transformer Temporal Head. A deep-learning pipeline that extracts frame-level features from video, processes them through a transformer model, and predicts accident probability in real-time with visual overlays.
